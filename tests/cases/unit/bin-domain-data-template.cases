# 도메인 단위: 데이터 파일 템플릿 (기능 001 cli_domain_plan_test_data_template_bytes, contracts/files.md 3.2절)
target bin-func ppg__domain_data_template
layer domain

case data-template-bytes
status 0
out # project-path-gateway 데이터 파일
out # 한 줄에 KEY=경로 형식으로 적습니다. 경로는 프로젝트 루트(.tools/의 상위 디렉터리) 기준 상대경로입니다.
out # 키: 대문자로 시작하고 대문자·숫자·_만 사용합니다. 파일 안에서 중복될 수 없습니다.
out # 경로: /로 시작할 수 없고 .. 세그먼트를 쓸 수 없습니다. = 앞뒤 공백은 제거되지 않습니다.
out # 빈 줄과 #으로 시작하는 줄은 무시합니다.
out #
out # 예:
out # SOURCES_ROOT=sources
out # BUILD_SCRIPT=scripts/build.sh
